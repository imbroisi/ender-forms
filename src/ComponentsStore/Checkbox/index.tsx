import React from 'react';
import {
  CheckboxContainer,
  ICheckboxContainerProps,
} from '../../FormFieldsBase';

export const Checkbox = (props: ICheckboxContainerProps) => (
  <CheckboxContainer {...props} />
)
